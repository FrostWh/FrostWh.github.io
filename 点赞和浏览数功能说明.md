# 点赞和浏览数功能使用说明

## 功能概述

已为你的 Hexo + Fluid 博客添加了以下功能：

1. **浏览数统计** - 显示每篇文章的浏览次数
2. **点赞功能** - 允许访客为文章点赞

## 配置说明

### 1. 浏览数统计

已在 `_config.fluid.yml` 中配置：

```yaml
post:
  meta:
    views:
      enable: true
      source: "busuanzi"  # 使用不蒜子统计（简单，无需配置）
```

**选项说明：**
- `busuanzi` - 简单易用，无需注册，但可能偶尔不稳定
- `leancloud` - 需要配置，但更稳定（需要填写 app_id 和 app_key）
- `umami` - 需要自部署 Umami 服务

### 2. 点赞功能

已在 `_config.fluid.yml` 中配置：

```yaml
post:
  meta:
    likes:
      enable: true
      source: "localStorage"  # 使用本地存储（简单，但只能本地）
```

**选项说明：**
- `localStorage` - 使用浏览器本地存储，简单但只能在同一设备上记录
- `leancloud` - 使用 LeanCloud 存储，可跨设备同步（需要配置）

## 文件说明

已创建以下文件：

1. `source/js/like-button.js` - 点赞功能的 JavaScript 实现
2. `source/css/like-button.css` - 点赞按钮的样式文件

## 集成步骤

### 方法一：通过主题注入（推荐）

如果 Fluid 主题支持自定义注入，可以在主题配置中添加：

```yaml
# 在 _config.fluid.yml 中添加（如果主题支持）
custom_inject:
  head:
    - <link rel="stylesheet" href="/css/like-button.css">
  bottom:
    - <script src="/js/like-button.js"></script>
```

### 方法二：手动添加到主题模板

如果方法一不可用，需要手动修改主题文件：

1. 找到主题的 `layout/_partials/post/post.ejs` 或类似文件
2. 在文章内容后、评论前添加：

```html
<!-- 点赞按钮容器 -->
<div id="post-like-container"></div>
```

3. 在主题的 `layout/_partials/head.ejs` 或 `layout/_partials/css.ejs` 中添加：

```html
<link rel="stylesheet" href="/css/like-button.css">
```

4. 在主题的 `layout/_partials/scripts.ejs` 或类似文件底部添加：

```html
<script src="/js/like-button.js"></script>
```

### 方法三：使用 Hexo 注入插件

安装 `hexo-inject` 插件：

```bash
npm install hexo-inject --save
```

然后在 `_config.yml` 中添加：

```yaml
inject:
  head:
    - <link rel="stylesheet" href="/css/like-button.css">
  bottom:
    - <script src="/js/like-button.js"></script>
```

## 使用 LeanCloud 存储点赞数据（可选）

如果需要跨设备同步点赞数据：

1. **注册 LeanCloud 账号**
   - 访问：https://leancloud.cn/
   - 注册并创建应用

2. **获取 App ID 和 App Key**
   - 在 LeanCloud 控制台 → 应用 → 设置 → 应用 Keys

3. **创建数据表**
   - 在 LeanCloud 控制台 → 数据存储 → 创建 Class
   - Class 名称：`Like`
   - 添加字段：
     - `target` (String) - 页面路径
     - `count` (Number) - 点赞数

4. **配置权限**
   - 在 Class 的权限设置中：
     - `add_fields` - 允许所有用户
     - `create` - 允许所有用户
     - `read` - 允许所有用户
     - `update` - 允许所有用户

5. **更新配置**
   ```yaml
   web_analytics:
     enable: true
     leancloud:
       app_id: "你的 App ID"
       app_key: "你的 App Key"
       server_url: ""  # 仅中国大陆用户需要填写
   
   post:
     meta:
       likes:
         enable: true
         source: "leancloud"
   ```

6. **更新 JavaScript**
   - 修改 `source/js/like-button.js` 中的 `useLeanCloud` 变量为 `true`，或从配置中读取

## 测试

1. 重新生成静态文件：
   ```bash
   hexo clean
   hexo generate
   ```

2. 启动本地服务器：
   ```bash
   hexo server
   ```

3. 访问文章页面，检查：
   - 文章元信息中是否显示浏览数
   - 文章底部是否显示点赞按钮
   - 点击点赞按钮是否正常工作

## 注意事项

1. **浏览数统计**：
   - 使用 `busuanzi` 时，本地运行可能显示很大的数字，这是正常现象
   - 部署到生产环境后会恢复正常

2. **点赞功能**：
   - 使用 `localStorage` 时，数据只存储在本地浏览器
   - 清除浏览器数据会丢失点赞记录
   - 如需跨设备同步，请使用 LeanCloud

3. **文件路径**：
   - 确保 `source/js/` 和 `source/css/` 目录中的文件会被正确复制到 `public/` 目录
   - 如果文件路径不对，请检查 Hexo 的配置

## 故障排除

1. **点赞按钮不显示**：
   - 检查浏览器控制台是否有错误
   - 确认 JavaScript 文件已正确加载
   - 确认 CSS 文件已正确加载

2. **浏览数不显示**：
   - 检查 `post.meta.views.enable` 是否为 `true`
   - 如果使用 LeanCloud，确认配置正确

3. **点赞数据不保存**：
   - 检查浏览器是否允许 localStorage
   - 如果使用 LeanCloud，检查网络请求是否成功

## 自定义样式

可以修改 `source/css/like-button.css` 来自定义点赞按钮的样式。

## 更多信息

- [Fluid 主题文档](https://hexo.fluid-dev.com/docs/)
- [LeanCloud 文档](https://leancloud.cn/docs/)
- [不蒜子统计](https://busuanzi.ibruce.info/)

